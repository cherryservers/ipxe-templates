#!ipxe
# Debian 13 (Trixie) netboot installer.
dhcp
set suite trixie
set arch amd64
set mirror http://deb.debian.org/debian/dists/${suite}/main/installer-${arch}/current/images/netboot/debian-installer/${arch}
kernel ${mirror}/linux ip=dhcp auto=true priority=critical vga=788 locale=en_US
initrd ${mirror}/initrd.gz
boot
